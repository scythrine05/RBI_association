import React, { Component } from "react";
import { Container } from "react-bootstrap";

export default class polls extends Component {
  render() {
    return (
      <React.Fragment>
        <Container>
          <h1>These are Polls</h1>
        </Container>
      </React.Fragment>
    );
  }
}
